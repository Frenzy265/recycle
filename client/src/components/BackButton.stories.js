import React from "react";
import { BackButton } from "./BackButton";

export default {
  title: "Components/Backbutton",
  parameters: { layout: "centered" },
  component: BackButton,
};

export const backButton = () => <BackButton></BackButton>;
